version: "1.0"

services:
  front:
    image: node/front:1.0
    container_name: front
    networks:
      - petsplace
    ports:
      - "8081:8081/tcp"
    build:
      context: ./
      dockerfile: Dockerfile

# considera que a subrede docker "petsplace"
# já foi criada pelo back.
networks:
  petsplace:
    external: true
    name: petsplace

